# JobSpy Social Scraper System

**🚀 Полнофункциональная система для сбора и управления вакансиями LinkedIn**

**Текущий статус**: ✅ **Система полностью готова к работе!**
- 🔍 LinkedIn скрапер с расширенными фильтрами
- 🗄️ Supabase база данных с аутентификацией
- 🌐 Next.js веб-интерфейс с современным UI
- 🔗 API интеграция для запуска скрапера из браузера
- 📊 Интерактивная таблица с фильтрами и поиском
- ⚡ Прямая запись данных в базу без CSV файлов

## Описание проекта

**JobSpy Social Scraper System** - это полнофункциональная веб-система для автоматизированного сбора и управления вакансиями LinkedIn. Система включает в себя:

- **Веб-интерфейс** для удобного поиска и просмотра вакансий
- **Python скрапер** для сбора данных с LinkedIn
- **База данных Supabase** для хранения и управления данными
- **REST API** для интеграции всех компонентов

### Быстрый старт в 3 шага:
1. **Запустите веб-приложение**: `cd task-app && npm run dev`
2. **Настройте базу данных**: Откройте http://localhost:3000/database-setup.html
3. **Начните поиск**: Выберите профессию или введите запрос → нажмите "🔍 Search"

### Ключевые возможности:
- ⚡ **Мгновенный поиск** из браузера
- 🎯 **15 популярных профессий** для быстрого выбора
- 📋 **Широкая таблица** со всеми деталями вакансий
- 🔗 **Прямые ссылки** на оригинальные вакансии LinkedIn
- 🗄️ **Автоматическое сохранение** в облачную базу данных

## Как работает система

### Полный рабочий процесс

1. **Пользователь открывает веб-интерфейс** (http://localhost:3000)
2. **Автоматическое перенаправление** на dashboard (/dashboard)
3. **Просмотр существующих вакансий** из базы данных Supabase
4. **Поиск новых вакансий**:
   - Ввод ключевых слов в поле поиска
   - Или выбор из списка популярных профессий (15 вариантов)
   - Нажатие кнопки "🔍 Search" для запуска скрапера
5. **API вызывает Python скрапер** с заданными параметрами
6. **Скрапер собирает данные** с LinkedIn и сохраняет в Supabase
7. **Веб-интерфейс обновляется** и показывает новые вакансии

### Техническая архитектура

#### Frontend (Next.js + TypeScript)
- **Главная страница** (`app/page.tsx`) - перенаправление на dashboard
- **Dashboard** (`app/dashboard/page.tsx`) - отображение вакансий и поиск
- **Компоненты**:
  - `JobSearchFilters` - форма поиска с популярными профессиями
  - `TaskList` - таблица вакансий
  - `TaskRow` - строка вакансии с ссылкой
- **Хуки**: `useTaskManager` - управление данными вакансий
- **Стилизация**: Tailwind CSS + Shadcn/ui компоненты

#### Backend (Next.js API Routes)
- **`/api/scrape-jobs`** - запуск Python скрапера
- **`/api/db-status`** - проверка соединения с базой
- **`/api/init-db`** - инициализация базы данных

#### Database (Supabase)
- **Таблица `scraped_jobs`** - хранение вакансий
- **Индексы** для быстрого поиска по company, location, search_query
- **Row Level Security** для защиты данных

#### Scraper (Python)
- **Основной скрипт**: `JobSpy/linkedin_jobs_search_scraper.py`
- **Интеграция с Supabase**: прямая запись в базу (без CSV)
- **Фильтры**: поддержка всех параметров JobSpy
- **Сессии**: стабильные HTTP запросы

## Что было сделано

### Основные изменения
- **Прямая интеграция с Supabase** - данные сохраняются сразу в базу, без промежуточных CSV файлов
- **Веб-интерфейс** - полнофункциональный dashboard для управления вакансиями
- **API интеграция** - запуск скрапера из браузера с пользовательскими параметрами
- **Удобный поиск** - форма с автозаполнением из популярных профессий
- **Широкая таблица** - оптимизированный интерфейс для просмотра всех данных
- **Автоматическое обновление** - результаты поиска сразу отображаются в интерфейсе

### Улучшения интерфейса
- **Популярные профессии**: 15 готовых запросов для быстрого поиска
- **Широкая таблица**: max-width увеличен до 7xl (1280px)
- **Иконка поиска**: кнопка с лупой вместо треугольника
- **Responsive дизайн**: адаптивный интерфейс для разных экранов
- **Фильтры**: поиск по компании, локации, типу работы и дате

### Интеграция с Supabase и веб-интерфейсом
- **База данных**: Полная интеграция с Supabase (PostgreSQL + Auth + Storage)
- **Веб-интерфейс**: Next.js приложение для просмотра и управления вакансиями
- **Аутентификация**: Упрощенная система (временно отключена для тестирования)
- **Хранение данных**: Структурированное хранение вакансий с метаданными
- **API**: REST API для взаимодействия скрапера и веб-интерфейса

## Структура файлов

```
JobSpy/
├── linkedin_jobs_search_scraper.py    # Основной скрипт скрапера
├── jobspy/
│   ├── linkedin/
│   │   ├── filters.py                 # Все фильтры и константы
│   │   ├── constant.py                # Заголовки HTTP запросов
│   │   ├── util.py                    # Утилиты JobSpy
│   │   └── __init__.py               # Основной LinkedIn скрейпер
│   └── model.py                       # Модели данных (Country, JobType)
└── README_LINKEDIN_SCRAPER.md         # Это руководство
```

### Архитектура системы

```
JobSpy Social Scraper System/
├── JobSpy/                          # LinkedIn скрапер
│   ├── linkedin_jobs_search_scraper.py
│   └── jobspy/linkedin/
├── linkedin_scraper_supabase.py     # Интеграция с Supabase
├── task-app/                        # Веб-интерфейс
│   ├── app/                         # Next.js страницы
│   ├── components/                  # React компоненты
│   ├── hooks/                       # Хуки для работы с данными
│   └── supabase/migrations/         # SQL миграции
└── create_linkedin_scraper_tables.sql # Схема базы данных
```

#### Компоненты системы:
1. **LinkedIn Scraper**: Сбор данных с поддержкой всех фильтров
2. **Supabase Database**: Хранение и управление данными
3. **Next.js Frontend**: Веб-интерфейс для пользователей
4. **API интеграция**: Синхронизация скрапера с базой данных

## Настройка и использование

### Быстрый старт

#### 1. Запуск веб-интерфейса
```bash
# Переход в директорию веб-приложения
cd task-app

# Установка зависимостей (если не установлены)
npm install

# Запуск в режиме разработки
npm run dev
# Приложение будет доступно на http://localhost:3000
```

#### 2. Настройка базы данных
1. Откройте http://localhost:3000/database-setup.html
2. Скопируйте SQL код и выполните в [Supabase SQL Editor](https://supabase.com/dashboard/project/your-project/sql)
3. Перейдите в дашборд для просмотра данных

#### 3. Использование системы
1. **Просмотр вакансий**: Откройте http://localhost:3000 - автоматически перенаправит на dashboard
2. **Поиск вакансий**:
   - Введите ключевые слова (например, "frontend developer")
   - Или выберите из списка популярных профессий
   - Нажмите кнопку "🔍 Search"
3. **Результаты**: Новые вакансии появятся в таблице автоматически

### Ручной запуск скрапера (опционально)

```bash
# Активация виртуального окружения
cd /path/to/project
source venv_jobspy/bin/activate

# Запуск скрапера с параметрами
python JobSpy/linkedin_jobs_search_scraper.py
```

### Настройка фильтров

Все настройки находятся в начале файла `linkedin_jobs_search_scraper.py`:

```python
# Базовые настройки
search_term = "data scientist"     # Ключевые слова для поиска
country_code = "USA"               # Код страны (см. filters.py)
distance = 50                      # Радиус поиска в км
is_remote = False                  # Только удаленная работа
job_type = "INTERNSHIP"           # Тип работы
easy_apply = True                 # Только Easy Apply
hours_old = None                  # Вакансии не старше N часов
results_wanted = 30               # Количество вакансий
```

## Доступные фильтры

### Страны
Используются из `Country` enum (`jobspy/model.py`):
- `USA`, `UK`, `DE`, `FR`, `ES`, `IT`, `NL`, `CA`, `AU`, `UA`, `PL`, `SE` и др.
- Полный список в `jobspy/linkedin/filters.py` → `COUNTRY_MAPPING`

### Типы работы
Из `JobType` enum (`jobspy/model.py`):
- `FULL_TIME` - Полная занятость
- `PART_TIME` - Частичная занятость
- `CONTRACT` - Контракт
- `TEMPORARY` - Временная работа
- `INTERNSHIP` - Стажировка
- `PER_DIEM` - Почасовая оплата
- Полный список в `jobspy/linkedin/filters.py` → `JOB_TYPE_MAPPING`

### Дополнительные фильтры
- `distance`: 25, 50, 100 км (или `None`)
- `is_remote`: `True`/`False`
- `easy_apply`: `True`/`False`
- `hours_old`: 24, 72, 168, 720 часов (или `None`)

## Как подключаются фильтры

### 1. Импорт фильтров
```python
from jobspy.linkedin.filters import COUNTRY_MAPPING, JOB_TYPE_MAPPING, DISTANCE_OPTIONS, HOURS_OLD_OPTIONS
```

### 2. Использование в коде
```python
# Получение объекта Country
country_obj = COUNTRY_MAPPING[country_code]
location = country_obj.value[0].split(",")[0].title()

# Получение объекта JobType
job_type_obj = JOB_TYPE_MAPPING.get(job_type)
if job_type_obj:
    params["f_JT"] = job_type_code(job_type_obj)
```

## HTTP Headers

### Как вызывается header
Headers автоматически подключаются из `jobspy/linkedin/constant.py`:

```python
from jobspy.linkedin.constant import headers

# Создание сессии с headers
session = create_session(
    is_tls=False,
    has_retry=True,
    delay=5,
    clear_cookies=True,
)
session.headers.update(headers)
```

### Структура headers
```python
headers = {
    "authority": "www.linkedin.com",
    "accept": "text/html,application/xhtml+xml,...",
    "accept-language": "en-US,en;q=0.9",
    "cache-control": "max-age=0",
    "upgrade-insecure-requests": "1",
    "user-agent": "Mozilla/5.0 (Macintosh; Intel Mac OS X 10_15_7)...",
}
```

## Результаты

### Веб-интерфейс
- **Автоматическое обновление**: Результаты поиска сразу отображаются в таблице
- **Широкая таблица**: Все колонки видны без прокрутки
- **Интерактивные ссылки**: Клик по ссылке открывает вакансию на LinkedIn
- **Фильтры**: Поиск и фильтрация по компании, локации, дате

### База данных
- **Прямая запись**: Данные сохраняются сразу в Supabase (таблица `scraped_jobs`)
- **Метаданные**: Хранятся поисковый запрос, дата сбора, уникальный ID вакансии
- **Дедупликация**: Избегание дубликатов по `linkedin_job_id`
- **Индексы**: Быстрый поиск по company, location, search_query, created_at

### Логи скрапера
При запуске из веб-интерфейса выводится подробная информация:
```
=== НАСТРОЙКИ ПОИСКА ===
Ключевые слова: frontend developer
Страна: USA
Тип работы: Любая
Желаемое количество вакансий: 30
==============================
Saved 10 jobs to database...
Saved 20 jobs to database...
Saved 30 jobs to database...
Собрано 30 вакансий
Сохранено в базу данных: 30 вакансий
```

## Примеры использования

### Удаленные вакансии Data Scientist в Германии
```python
search_term = "data scientist"
country_code = "DE"
is_remote = True
```

### Стажировки по Python в США с Easy Apply
```python
search_term = "python internship"
country_code = "USA"
job_type = "INTERNSHIP"
easy_apply = True
```

### Контрактная работа в радиусе 50 км
```python
job_type = "CONTRACT"
distance = 50
```

## Технические детали

- **Сессия**: Использует `create_session` из JobSpy для стабильных запросов
- **Retry логика**: Автоматические повторы при ошибках
- **Пагинация**: Обрабатывает до 1000 результатов (ограничение LinkedIn)
- **Дедупликация**: Избегает повторяющихся вакансий по ID
- **Задержки**: Случайные задержки 1-5 секунд между запросами

## Использование веб-интерфейса

### Поиск вакансий
1. **Быстрый поиск**: Выберите профессию из списка популярных (15 вариантов)
2. **Пользовательский поиск**: Введите свои ключевые слова в поле поиска
3. **Запуск скрапера**: Нажмите кнопку "🔍 Search"
4. **Ожидание результатов**: Процесс занимает 1-3 минуты в зависимости от количества вакансий

### Просмотр результатов
- **Таблица вакансий**: Название, компания, локация, поисковый запрос, дата
- **Сортировка**: По дате добавления (новые сверху)
- **Фильтрация**: Поиск по существующим вакансиям без запуска скрапера
- **Ссылки**: Клик по иконке → открытие вакансии на LinkedIn

### Управление данными
- **Автоматические обновления**: Новые вакансии появляются сразу после сбора
- **История поиска**: Все запросы сохраняются в базе данных
- **Дедупликация**: Повторяющиеся вакансии автоматически исключаются

## Зависимости

### Python (для скрапера)
- `requests` - HTTP запросы
- `beautifulsoup4` - Парсинг HTML
- `supabase-py` - Работа с Supabase
- JobSpy библиотека (в виртуальном окружении)

### Node.js (для веб-интерфейса)
- `next.js` - React фреймворк
- `typescript` - Типизированный JavaScript
- `@supabase/supabase-js` - Supabase клиент
- `tailwindcss` - CSS фреймворк
- `lucide-react` - Иконки
- `@radix-ui/react-select` - UI компоненты

## Интеграция скрапера с Supabase

### Как работает сохранение данных

Скрапер (`linkedin_jobs_search_scraper.py`) напрямую сохраняет данные в Supabase:

```python
# Инициализация Supabase клиента
supabase = create_client(SUPABASE_URL, SUPABASE_KEY)

# Сбор данных с LinkedIn
jobs_data = scrape_linkedin_jobs(search_term, country_code, ...)

# Прямая запись в базу
for job in jobs_data:
    save_job_to_supabase(job, search_params)
```

### Схема базы данных

**Таблица `scraped_jobs`:**
- `job_id` (UUID, Primary Key) - Уникальный ID вакансии
- `linkedin_job_id` (TEXT, UNIQUE) - ID вакансии на LinkedIn
- `title` (TEXT, NOT NULL) - Название вакансии
- `company` (TEXT, NOT NULL) - Название компании
- `location` (TEXT) - Локация
- `date_posted_text` (TEXT) - Дата публикации
- `logo_url` (TEXT) - URL логотипа компании
- `job_url` (TEXT, UNIQUE) - Ссылка на вакансию
- `search_query` (TEXT) - Поисковый запрос
- `scraped_at` (TIMESTAMP) - Время сбора данных
- `created_at` (TIMESTAMP) - Время создания записи
- `updated_at` (TIMESTAMP) - Время обновления

### API Endpoints

#### `/api/scrape-jobs` (POST)
Запуск скрапера с пользовательскими параметрами:
```json
{
  "search_term": "frontend developer",
  "country": "USA",
  "job_type": "FULL_TIME",
  "results_wanted": 30
}
```

#### `/api/db-status` (GET)
Проверка соединения с базой данных

#### `/api/init-db` (POST)
Инициализация базы данных и выполнение миграций

### Row Level Security
```sql
-- Доступ только для аутентифицированных пользователей
CREATE POLICY "Authenticated users can view scraped jobs"
  ON public.scraped_jobs FOR SELECT USING (auth.role() = 'authenticated');

CREATE POLICY "Authenticated users can insert scraped jobs"
  ON public.scraped_jobs FOR INSERT WITH CHECK (auth.role() = 'authenticated');
```

## Развертывание на сервере

### Настройка продакшена

#### Исправление хардкор путей
**ВНИМАНИЕ**: В файле `task-app/app/api/scrape-jobs/route.ts` строка 46 содержит хардкор путь:

```typescript
const venvPath = '/Users/alekfilippov/Documents/project/job_LI/venv_jobspy/bin/activate';
```

**На сервере нужно заменить на правильный путь к виртуальному окружению Python.**

Пример для Linux сервера:
```typescript
const venvPath = '/home/username/scanrole/venv_jobspy/bin/activate';
```

Или использовать переменную окружения:
```typescript
const venvPath = process.env.PYTHON_VENV_PATH || '/path/to/venv/bin/activate';
```

### Шаги развертывания

1. **Клонировать репозиторий**:
```bash
git clone https://github.com/AlexFilippov-it/scanrole.git
cd scanrole
```

2. **Настроить виртуальное окружение Python**:
```bash
python3 -m venv venv_jobspy
source venv_jobspy/bin/activate
pip install -r requirements.txt
```

3. **Установить зависимости Node.js**:
```bash
cd task-app
npm install
```

4. **Исправить путь в route.ts** (строка 46)

5. **Настроить переменные окружения**:
```bash
cp task-app/.env.example task-app/.env.local
# Заполнить переменные Supabase
```

6. **Запустить приложение**:
```bash
npm run build
npm start
```

## Важные замечания

### Использование
- **Соблюдайте правила LinkedIn**: Не превышайте разумные лимиты запросов
- **Задержки**: Система автоматически использует случайные задержки 1-5 секунд
- **Региональные ограничения**: Результаты зависят от региона и времени суток
- **Обновление данных**: Вакансии могут дублироваться при повторных поисках

### Производительность
- **Ограничения LinkedIn**: Максимум 1000 результатов на запрос
- **Скорость сбора**: 30 вакансий занимают 1-3 минуты
- **Память**: Скрапер оптимизирован для работы с большими объемами данных

### Безопасность и приватность
- **Row Level Security**: Доступ только для аутентифицированных пользователей
- **Индексы**: Быстрый поиск без нагрузки на базу данных
- **Логирование**: Все действия записываются для отладки

### Развитие системы
- **Модульная архитектура**: Легко добавлять новые источники вакансий
- **API-first подход**: Все функции доступны через REST API
- **TypeScript**: Полная типизация для надежности кода
- **Современный UI**: Адаптивный интерфейс с лучшими практиками UX

## Статус проекта

✅ **Система полностью готова к использованию!**

- Веб-интерфейс работает на http://localhost:3000
- Скрапер интегрирован с Supabase
- Поиск вакансий работает из браузера
- Данные сохраняются и отображаются корректно
- Интерфейс оптимизирован для удобства использования
